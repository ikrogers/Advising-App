<% @currentuser = User.find_by_id(session[:user_id]) %>

<div>
	<%if @currentuser.flag != 'submitted' && @currentuser.flag != 'approved'%>
		<h1>Registration options for <%= @currentuser.name %> </h1>
		<br>
		<%= render 'bootstrap_dual_list' %>
	</div>>
	<%else%>
		<h1>Submitted registrion package for <%= @currentuser.name %> </h1>
		<br>
		
		<%@regcourses = Course.find_all_by_studentid(@currentuser.id)%> 
		<% @regcourses.each do |c| %>
      		<%if(c.choice == 'completed')%>
      	<%end%>
      	<% @regcourses.each do |c| %>
      		<%if(c.choice == 'registered')%>
      		
      	<%end%>
	<%end%>
	
	
<br>	
<div>

<%case @currentuser.classification%>
<%      when "Admin"%>
<%= link_to 'New Course', new_course_path %>
<%= link_to 'Add to Courselist', new_courselist_path %>
<%= link_to 'Back', admin_path() %>
<%      when "Advisor"%>
<%= link_to 'Back', advisor_path(),:class => "btn btn-outline-inverse btn-block" %>
<%     else%>
<%= link_to 'Back', student_path(),:class => "btn btn-outline-inverse btn-block" %>
<%    end%>
<%    end%>
<%    end%>
</div>

