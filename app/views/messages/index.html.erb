<%@currentuser = User.find_by_id(session[:user_id])%>
<h1><%=@currentuser.name%>'s Messages</h1>
<table>


  <tbody>
  	<%if(@currentuser.classification == 'Student')%>
    <% @messages.each do |message| %>
      <%if(message.to== @currentuser.id)%>
      <div>
      <tr>
        <td><%= "From: #{@currentuser.advisor}  "%></td>
      </tr>
      <tr>
        <td><%= message.content %></td>
      </tr>
      </div>
      <%elsif(message.from == User.find_by_name(@currentuser.advisor).id)%>
      <div>
      <tr>
        <td><%= "To: #{@currentuser.name}  "%></td>
      </tr>
      <tr>
        <td><%= message.content %></td>
      </tr>
      </div>
    <% end %>
    <% end %>
    <%else%>
    <% @messages.each do |message| %>
      <%if(message.to == @currentuser.id)%>
      <tr>
        <td><%= "From: #{@currentuser.advisor}  "%></td>
      </tr>
      <tr>
        <td><%= message.content %></td>
      </tr>
      <%elsif(message.from == @currentuser.id && message.to == @fromuser)%>
      <div>
      <tr>
        <td><%= "To: #{@currentuser.name}  "%></td>
      </tr>
      <tr>
        <td><%= message.content %></td>
      </tr>
      </div>
    <% end %>
    <% end %>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Message', new_message_path %>
<%= link_to 'Back', student_path %>